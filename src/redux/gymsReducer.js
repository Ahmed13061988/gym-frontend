
const initialGymState = {
    gyms: []
  }
  
  const gymsReducer = (state= initialGymState, action) =>{
    switch (action.type){
        default: 
        return {...state}
    }
  }


  export default gymsReducer; 